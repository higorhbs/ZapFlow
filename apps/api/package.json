{
  "name": "@flowdesk/api",
  "version": "0.1.0",
  "main": "dist/firebase-function.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "dev": "pnpm --filter @flowdesk/firebase build && pnpm --filter @flowdesk/shared build && tsx watch src/index.ts",
    "build": "pnpm --filter @flowdesk/firebase build && pnpm --filter @flowdesk/shared build && tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/formbody": "^7.4.0",
    "@flowdesk/firebase": "workspace:*",
    "@flowdesk/shared": "workspace:*",
    "@flowdesk/whatsapp-client": "workspace:*",
    "axios": "^1.7.7",
    "date-fns": "^3.6.0",
    "fastify": "^4.28.1",
    "fastify-raw-body": "^4.3.0",
    "firebase-functions": "^6.0.1",
    "stripe": "^22.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "tsx": "^4.19.1",
    "typescript": "^5.5.0"
  }
}
